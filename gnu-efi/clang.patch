--- a/Make.defaults
+++ b/Make.defaults
@@ -51,8 +51,8 @@
 INSTALL	     := install
 
 # Compilation tools
-HOSTCC       := $(prefix)gcc
-CC           := $(prefix)$(CROSS_COMPILE)gcc
+HOSTCC       := $(prefix)clang
+CC           := $(prefix)$(CROSS_COMPILE)clang
 AS           := $(prefix)$(CROSS_COMPILE)as
 LD           := $(prefix)$(CROSS_COMPILE)ld
 AR           := $(prefix)$(CROSS_COMPILE)ar
@@ -174,7 +174,6 @@
            -fno-stack-check
 endif
 
-ARFLAGS += -U
 ASFLAGS += $(ARCH3264)
 LDFLAGS	+= -nostdlib --warn-common --no-undefined --fatal-warnings \
 	   --build-id=sha1
